import
{{titleCase name}}
from "../models/{{snakeCase name}}.model.js"; export function create{{titleCase
  name
}}(input) { return
{{titleCase name}}.create(input); } export function find{{titleCase
  name
}}ById(id) { return
{{titleCase name}}.findById(id); }