//
{{titleCase name}}
Schema import mongoose from "mongoose"; const
{{name}}Schema = new mongoose.Schema( { }, { timestamps: true, } ); const
{{titleCase name}}
= mongoose.model("{{titleCase name}}",
{{name}}Schema); export default
{{titleCase name}};